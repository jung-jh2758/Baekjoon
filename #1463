x=int(input())
d=[0]*(x+1)
for i in range(2,x+1):
    d[i]=d[i-1]+1
    if i%3==0:
        d[i]=min(d[i],d[i//3]+1)
    if i%2==0:
        d[i]=min(d[i],d[i//2]+1)
print(d[x])

#알고리즘 생각 후 점화식
#다이나믹 프로그래밍
#반복문 사용, 재귀문 미사용
#바텀업 DP
#1부터 x까지 차례대로 d[i] 계산
#print([x]) 아니고 print(d[x])
#d[1+1]=d[i]+1을 쓰면 dp 테이블이 오염됨 i 값이 제대로 설정되기도 전에 i+1값을 확정해버림
